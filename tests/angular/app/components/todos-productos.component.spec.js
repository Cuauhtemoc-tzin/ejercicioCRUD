ngDescribe({
    name: 'Test todos-productos component',
    modules: 'app',
    element: '<todos-productos></todos-productos>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
